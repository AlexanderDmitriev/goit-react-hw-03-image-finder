import {ModalOverlay,ModalContent} from './Modal.styled';

const Modal = () => {
    return (
            <ModalOverlay>
                <ModalContent>
                    <img src="" alt="oops" />
                </ModalContent>
            </ModalOverlay>
    );
};

export default Modal;